# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.zed

# src/bindings.ts is auto-generated (cargo test regenerates it) but IS
# committed: the frontend can't typecheck or build without it, and neither
# CI's frontend job nor the release runners compile Rust first. CI fails
# if the committed copy drifts from the command list.

src-tauri/target/

# Design export drops (temporary, extracted into scratch space)
raw/

# VitePress (docs site) build output and cache
docs/.vitepress/dist/
docs/.vitepress/cache/
